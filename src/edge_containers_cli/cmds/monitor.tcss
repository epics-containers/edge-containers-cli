Footer {
    background: transparent;
}

Footer > .footer--key {
  background: rgb(30, 144, 255);
}

IocTable {
  height: 1fr;
}

.narrow IocTable {
  height: 100%;
}

IocTable > ScrollView {
  height: 100%;
}

Button {
    width: 16;
    height: 1fr;
    content-align: center middle;
    text-align: center;
    margin-left: 1;
    margin-right: 1;
}