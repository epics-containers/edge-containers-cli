# Trunk comment
trunk_A:
  # branch comment
  branch_A:
    leaf_A: 0 # Leaf comment
    leaf_B: zero
    leaf_C: # None
  branch_B: # Empty entry
trunk_B:
  leaf_B: true # Out of order
  leaf_A: false # Comment before space

leaf_A: "False"
